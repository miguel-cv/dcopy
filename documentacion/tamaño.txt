 
for i:=1 to ParamCount do
   begin
   if not (DirectoryExists(ParamStr(i))) then
    begin
    write(ParamStr(i));
   System.Assign (F,ParamStr(i));
   Reset (F);
   Writeln (' - File size in bytes : ',FileSize(F));
   Close (F);
    end;
   end ;
     Terminate;
  Exit;    